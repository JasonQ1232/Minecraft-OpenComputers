local sides = require("sides")
local settings = {
    altars = {
        altar_addresses = {""},
        altar_transposers = {""},
    },
    altar_transfer_count = 1,
    altar_min_capacity = 2500,
    debug = true
}
